<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- 탭 제목 -->
    <TextView
        android:id="@+id/tvCropTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="작물 관리 화면"
        android:textSize="24sp"
        android:textStyle="bold"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 탭 버튼 4개 -->
    <Button
        android:id="@+id/btnInitWork"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="초기 작업"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/tvCropTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/btnManage"
        app:layout_constraintWidth_percent="0.25"/>

    <Button
        android:id="@+id/btnManage"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="작물 관리"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/tvCropTitle"
        app:layout_constraintStart_toEndOf="@id/btnInitWork"
        app:layout_constraintEnd_toStartOf="@id/btnSell"
        app:layout_constraintWidth_percent="0.25"/>

    <Button
        android:id="@+id/btnSell"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="판매"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/tvCropTitle"
        app:layout_constraintStart_toEndOf="@id/btnManage"
        app:layout_constraintEnd_toStartOf="@id/btnMyPage"
        app:layout_constraintWidth_percent="0.25"/>

    <Button
        android:id="@+id/btnMyPage"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="마이페이지"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/tvCropTitle"
        app:layout_constraintStart_toEndOf="@id/btnSell"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.25"/>

    <!-- 초기 작업 결과 (작물 이름 및 시간 출력) -->
    <TextView
        android:id="@+id/textViewCropInfo"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="작물 정보가 여기에 표시됩니다."
        android:textSize="16sp"
        android:padding="8dp"
        android:background="#f0f0f0"
        android:textColor="#000000"
        app:layout_constraintTop_toBottomOf="@id/btnInitWork"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />


    <!-- 카메라 프리뷰 (기본 숨김) -->
    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/textViewCropInfo"
        app:layout_constraintBottom_toTopOf="@id/btnTakePhoto"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 찍은 사진 보여줄 곳 -->
    <ImageView
        android:id="@+id/imageViewPhoto"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:scaleType="fitCenter"
        android:background="#EEE"
        app:layout_constraintTop_toBottomOf="@id/textViewCropInfo"
        app:layout_constraintBottom_toTopOf="@id/btnTakePhoto"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 카메라 실행 및 촬영 버튼 -->
    <Button
        android:id="@+id/btnTakePhoto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="카메라 실행"
        android:layout_margin="16dp"
        app:layout_constraintTop_toBottomOf="@id/imageViewPhoto"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
